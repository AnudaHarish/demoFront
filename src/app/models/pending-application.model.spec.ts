import { PendingApplication } from './pending-application.model';

describe('PendingApplication', () => {
  it('should create an instance', () => {
    expect(new PendingApplication()).toBeTruthy();
  });
});
